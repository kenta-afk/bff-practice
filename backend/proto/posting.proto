syntax = "3";
package posting;

service Posting {
    rpc Posting (PostingRequest) returns (PostingResponse);
}

message PostingRequest {
    string name = 1;
    string message = 2;
}

message PostingResponse {
    string name = 1;
    string message = 2;
}